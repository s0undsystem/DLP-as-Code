# Dependencies are declared once, in pyproject.toml. This file installs the project itself in
# editable mode, which pulls them in, so `pip install -r requirements.txt` keeps working
# everywhere it is already used (the four workflows, the README, CONTRIBUTING) while there is
# only one place a version is pinned.
#
# `pip install -e .` is equivalent. For the Claude backend: `pip install -e '.[claude]'`.
-e .
